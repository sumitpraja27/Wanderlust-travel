<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="description"
        content="WanderLust - Find your perfect accommodation and travel experiences with enhanced accessibility features">
    <meta name="keywords" content="travel, accommodation, accessibility, inclusive travel, wanderlust">
    <meta name="author" content="WanderLust Team">
    <meta name="theme-color" content="#007bff">
    <title>
        <% if (typeof title !=='undefined' ) { %>
            <%= title %> - WanderLust<% } else { %>WanderLust - Accessible Travel Platform<% } %>
    </title>

    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/images/compass.png">
    <link rel="shortcut icon" type="image/png" href="/images/compass.png">
    <link rel="apple-touch-icon" href="/images/compass.png">

    <!-- Accessibility Preconnect -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://cdn.jsdelivr.net">

    <!-- EMERGENCY CACHE CLEAR - Loads FIRST to clean corrupted caches -->
    <script src="/JS/emergencyCacheClear.js?v=<%= Date.now() %>"></script>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
        integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />

    <!-- Custom CSS -->
    <link rel="stylesheet" href="/CSS/theme-system.css?v=<%= Date.now() %>">
    <link rel="stylesheet" href="/CSS/footer-theme.css?v=<%= Date.now() %>">
    <link rel="stylesheet" href="/CSS/navbar-theme.css?v=<%= Date.now() %>">
    <link rel="stylesheet" href="/CSS/style.css?v=<%= Date.now() %>">
    <link rel="stylesheet" href="/CSS/rating.css?v=<%= Date.now() %>">
    <link rel="stylesheet" href="/CSS/loading.css?v=<%= Date.now() %>">
    <link rel="stylesheet" href="/CSS/chatbot.css?v=<%= Date.now() %>">
    <link rel="stylesheet" href="/CSS/theme-toggle.css?v=<%= Date.now() %>">

    <!-- Mapbox CSS -->
    <link href='https://api.mapbox.com/mapbox-gl-js/v3.0.1/mapbox-gl.css' rel='stylesheet' />

    <!-- Structured Data for SEO -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "WebApplication",
      "name": "WanderLust",
      "description": "Accessible travel platform for finding accommodations and travel experiences",
      "url": "<%= process.env.BASE_URL || 'https://wanderlust.com' %>",
      "applicationCategory": "TravelApplication",
      "operatingSystem": "Web Browser",
      "accessibilityAPI": "ARIA",
      "accessibilityControl": "fullKeyboardControl,fullMouseControl,fullTouchControl",
      "accessibilityFeature": "alternativeText,captions,highContrastDisplay,largePrint,readingOrder,screenReader"
    }
    </script>

    <style>
        /* Smooth scrolling */
        html {
            scroll-behavior: smooth;
        }

        /* Prevent horizontal overflow */
        html,
        body {
            overflow-x: hidden !important;
            max-width: 100vw;
            position: relative;
        }

        /* Fix navbar overflow issues - minimal styling to avoid conflicts */
        .navbar {
            overflow-x: hidden !important;
            max-width: 100vw;
        }

        /* Offset content for fixed navbar */
        body {
            padding-top: 70px;
        }

        @media (max-width: 768px) {
            body {
                padding-top: 64px;
            }
        }

        /* Hide scrollbar but allow scrolling */
        .navbar::-webkit-scrollbar {
            display: none;
        }

        /* Responsive adjustments */
        @media (max-width: 1400px) {
            .navbar-nav .nav-link {
                font-size: 0.85rem;
            }
        }

        @media (max-width: 1200px) {
            .navbar-brand span {
                display: none;
            }

            .navbar-brand i {
                margin-right: 0 !important;
            }
        }

        @media (max-width: 991px) {
            .navbar .d-flex.flex-grow-1 {
                max-width: 100%;
                margin: 0.5rem 0 !important;
            }

            .navbar .container-fluid {
                flex-wrap: wrap;
            }
        }
    </style>
</head>

<body>
    <%- include("../includes/navbar.ejs") %>

        <!-- Skip Links (accessible navigation) -->
        <a class="skip-link sr-only-focusable" href="#main-content">Skip to main content</a>
        <a class="skip-link sr-only-focusable" href="#navigation">Skip to navigation</a>

        <!-- Main Content - Centered container matching navbar -->
        <main id="main-content" role="main" class="main-container">
            <%- include("../includes/flash.ejs") %>
                <%- body %>
        </main>

        <%- include("../includes/footer.ejs") %>
            <%- include("../includes/chatbot.ejs") %>

                <!-- JavaScript Libraries -->
                <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
                    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
                    crossorigin="anonymous"></script>

                <!-- PDF Library for Offline Functionality -->
                <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/3.0.3/jspdf.umd.min.js"></script>

                <!-- Custom JavaScript -->
                <script src="/JS/accessibility.js?v=<%= Date.now() %>"></script>
                <script src="/JS/lazy-loading.js?v=<%= Date.now() %>"></script>
                <script src="/JS/script.js?v=<%= Date.now() %>"></script>
                <script src="/JS/loading.js?v=<%= Date.now() %>"></script>
                <script src="/JS/phraseAssistant.js?v=<%= Date.now() %>"></script>
                <script src="/JS/offlineManager.js?v=<%= Date.now() %>"></script>
                <script src='https://api.mapbox.com/mapbox-gl-js/v3.0.1/mapbox-gl.js'></script>

                <!-- Development Accessibility Testing -->
                <% if (process.env.NODE_ENV==='development' ) { %>
                    <script src="/JS/accessibility-test.js?v=<%= Date.now() %>"></script>
                    <% } %>

                        <!-- Performance and Accessibility Monitoring -->
                        <script>
                            // Performance monitoring
                            window.addEventListener('load', function () {
                                setTimeout(function () {
                                    const perfData = performance.getEntriesByType('navigation')[0];
                                    if (perfData) {
                                        console.log('Page load time:', perfData.loadEventEnd - perfData.loadEventStart + 'ms');
                                    }
                                }, 0);
                            });

                            // Accessibility error reporting
                            window.addEventListener('error', function (e) {
                                if (e.error && e.error.message && e.error.message.includes('accessibility')) {
                                    console.warn('Accessibility error detected:', e.error.message);
                                }
                            });
                        </script>
</body>

</html>